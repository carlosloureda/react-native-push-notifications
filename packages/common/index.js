/*TODO: Add here your server url (if using localhost use ngrok to get a proper
    URL for the mobile app) */
const server_url = "http://ff66770f.ngrok.io";

export const MESSAGE_ENPOINT = `${server_url}/message`;
export const PUSH_REGISTRATION_ENDPOINT = `${server_url}/token`;
